services:
  - type: web
    name: it-ticket-system
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn it-ticket-system.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: "3.12.0"
    plan: free
